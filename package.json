{
  "name": "web-assistant",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "init": "bash scripts/init.sh",
    "dev": "pnpm --filter web-astryx run build && (pnpm --filter web-astryx run dev & pnpm --filter backend run dev)",
    "build": "pnpm --filter web-astryx run build",
    "deploy": "pnpm run build && pnpm --filter backend run deploy",
    "typecheck": "pnpm --filter web-astryx run typecheck && pnpm --filter backend run typecheck",
    "test": "pnpm --filter backend run test",
    "db:generate": "pnpm --filter backend run db:generate",
    "db:migrate": "pnpm --filter backend run db:migrate"
  }
}
